<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Pagamentos extends Model
{
    /**
     * COMANDOS ARTISAN
     * --------------------------------------------------
     * php artisan make:migrate create_nometabela_table
     * php artisan make:model Pessoas
     * php artisan make:controlle PessoasController 
     * php artisan make:controlle PessoasController --resource (cria a estruta do controller toda);
     * php artisan migrate (cria as tabelas no banco de dados)
     * php artisan migrate:rollback
     * -----------------------------------------------------------
     *      
     * */
    //protected $fillable = ['id','nome','endereco','telefone','bairro','numero','cep'];
}
